{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "apps/api/**/*.{js,ts}": [
      "eslint --fix",
      "prettier --write"
    ],
    "apps/ui/**/*.{js,jsx,ts,tsx}": [
      "eslint --fix",
      "prettier --write"
    ],
    "libs/**/*.{js,ts}": [
      "eslint --fix",
      "prettier --write"
    ]
  }
}
